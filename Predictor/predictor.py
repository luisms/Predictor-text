def readWords(file1):
	text = open(file1,'r+')
	readW = text.read()
	information = []
	readW= readW.split()
	frecuencia = dict()	
	for i in range(len (readW)):
		if (i in frecuencia):	
	    		frecuencia.
		else:
	return (information)

(readWords("texto"))
